class CWitcherTrophy extends CItemEntity
{
	/*event OnDetach( parentEntity : CEntity )
	{
		.var hook : SItemUniqueId;
		hook = thePlayer.GetInventory().GetItemByCategory('hook');
		thePlayer.GetInventory().UnmountItem(hook);
	}
	event OnMount( parentEntity : CEntity, slot : name )
	{
		var hook : SItemUniqueId;
		hook = thePlayer.GetInventory().GetItemByCategory('hook');
		thePlayer.GetInventory().MountItem(hook);
	}*/

}